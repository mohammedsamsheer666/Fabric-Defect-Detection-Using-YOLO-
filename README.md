# fabricdetetion
This a fabric detection code using python that will detect all the type of defects in fabric by using you onlu look onece mechanism with labels.
Activate the environment
env/Scripts/virtualenv
pip freeze -r requirements.txt \\ In order to update the txt file from the data fetched while activation.

pip install -r requirements.txt \\ to bring the package from pip to the system folder.